name: Deploy Todo API

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.ROBSTEP4LIFE }}
          password: ${{ secrets.Robstep6768@ }}

      - name: Build Docker image
        run: docker build -t ${{ secrets.robstep4life  }}/todo-api:latest .

      - name: Push Docker image
        run: docker push ${{ secrets.Robstep6768@ }}/todo-api:latest
